// import { midi } from './properties';

// export const distance = (...args) => {
//   const [first, second, fn] = args;
//   const { length } = args;
//   if (length === 1) return (second, fn = midi) => distance(first, second, fn);
//   if (length === 2) return (fn = midi) => distance(first, second, fn);
//   return Math.abs(fn(first) - fn(second))

// }
