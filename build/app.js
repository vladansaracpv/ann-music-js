"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// NOTE_PATTERN = re.compile(
//     r"^(?P<letter>[a-gA-G]?)"
//     r"(?P<accidental>#{1,}|b{1,}|x{1,}|)"
//     r"(?P<octave>-?\d*)\s*(?P<rest>.*)$")
var KEYS = ['name', 'letter', 'accidental', 'octave', 'pc', 'step', 'alteration', 'chroma', 'midi', 'frequency'];
console.log('asd');
// const NO_NOTE = 
// NO_NOTE = dict.fromkeys(KEYS)
// ALL_NOTES = NOTES
// SHARPS_ONLY = list(filter(lambda x: "#".find(
//     " " if len(x) == 1 else x[1]) != -1, NOTES))
// FLATS_ONLY = list(filter(lambda x: "b".find(
//     " " if len(x) == 1 else x[1]) != -1, NOTES))
// NATURALS_ONLY = list(filter(lambda x: " ".find(
//     " " if len(x) == 1 else x[1]) != -1, NOTES))
// SHARPS = list(filter(lambda x: " #".find(
//     " " if len(x) == 1 else x[1]) != -1, NOTES))
// FLATS = list(filter(lambda x: " b".find(
//     " " if len(x) == 1 else x[1]) != -1, NOTES))
//# sourceMappingURL=app.js.map